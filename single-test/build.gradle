plugins {
    id 'java'
}

group 'org.springframework'
version '5.1.15.BUILD-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    // 依赖 spring-context 模块
    compile(project(":spring-context"))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
